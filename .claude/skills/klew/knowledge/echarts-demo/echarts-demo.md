---
app: echarts-demo
updated: 2026-07-22
reconciled_signature: sha256:6ad413c74549dbf1
test_attribute: data-test
base_url: http://127.0.0.1:8123
---

# echarts-demo — app knowledge

ECharts 6 (2D canvas), bars addressed by category name sample for the klew **scene tier** (see `references/selector-policy.md`). The
shapes have no DOM element, so they are cached as `tier: scene` and clicked via
the app's own scene model — never a hardcoded pixel. Sample: `e2e/scene/echarts.html`.

## Notes & gotchas

- Requires the scene instance (`window.__echart`) to be exposed.
- Every scene entry is `a11y_flag: true` — canvas shapes are absent from the
  accessibility tree.
- Act via `scripts/scene_click.py` or the generated `echarts-demo.pom.ts`.

## Application map

_Auto-generated by `knowledge_scaffold.py` — do not edit between the `klew:auto` markers._

<!-- klew:auto:start pages -->
| Route | Selectors |
| ----- | --------- |
| `/echarts.html` | 2 |
<!-- klew:auto:end pages -->

<!-- klew:auto:start a11y -->
- `bars.apr` (tier=scene) — non-user-facing locator; likely missing an accessible role/name.
- `bars.feb` (tier=scene) — non-user-facing locator; likely missing an accessible role/name.
<!-- klew:auto:end a11y -->

<!-- klew:auto:start selectors:bars -->
- `bars.apr` → `scene:echarts/name=Apr` (scene · conf 0.7 · a11y)
- `bars.feb` → `scene:echarts/name=Feb` (scene · conf 0.7 · a11y)
<!-- klew:auto:end selectors:bars -->

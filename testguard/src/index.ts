/**
 * testguard — public entry point.
 * (Surface grows as milestones land; see CHECKLIST.md.)
 */

export type {
  TestFile,
  TestCase,
  Selector,
  Assertion,
  AsyncCall,
  Wait,
  TextArtifact,
  Finding,
  Severity,
} from "./model.js";
